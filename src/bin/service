#!/usr/bin/env python3

import psutil
import time
import requests

while True:
    time.sleep(1)
    cpu = psutil.cpu_percent(1)
    mem = psutil.virtual_memory().percent
    r = requests.post("http://10.1.0.1:8000/performance", data={'cpu': cpu, 'mem': mem})
